# Generate with:
# node -e "import b from 'bcryptjs'; console.log(b.hashSync('replace-me', 12))"
ADMIN_PASSWORD_HASH=
ADMIN_SESSION_SECRET=
ADMIN_COOKIE_SECURE=true
ADMIN_PORT=4180
ADMIN_HOST=127.0.0.1

# Optional: outbound email sender (Gmail SMTP/app-password or equivalent)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
MAIL_FROM=

# Optional for image generation in presentation engine
# OPENAI_API_KEY=
# GEMINI_API_KEY=
# XAI_API_KEY=
# ANTHROPIC_API_KEY=
