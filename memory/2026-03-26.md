# 2026-03-26

- Fixed Signals Feed refresh endpoint reliability in `admin-server`:
  - Root bug: refresh route used `exec(...)` without proper import, causing refresh-path instability/502 symptoms.
  - Replaced with synchronous `spawnSync` execution + redirect status flags (`?refresh=ok|err`) and error-safe handling.
  - Added role-aware refresh control (disabled button for non `architect/editor`) and inline refresh status banner.

- Clarified permissions/debug path for refresh failures:
  - `POST /api/signals-feed/refresh` requires `architect` or `editor` role.
  - Added/used `/api/me` check guidance to verify runtime `effectiveRole` when user sees forbidden behavior.

- Created and populated new initiative:
  - `INIT-2026-03-ECOWAS-DIGITAL-SAFETY-INFRASTRUCTURE` (`DPSI-001`).
  - Built with full initiative schema depth (multi-layer actor alignment, readiness fields, constraints, transition metadata).
  - Positioning decision: frame as digital public safety infrastructure/gov modernization (not "robot police" framing).

- Added new actors + initiative mappings:
  - `momodu-conteh-international-human-rights-commission` mapped into ECOWAS initiative political layer as regional envoy liaison.
  - `chelsia-mckie-workforce-access-network` mapped into ECOWAS initiative development/delivery layers for workforce pipeline design + student talent channel.

- Reclassified AP AI Factory execution signal as internal constraint-unblock action:
  - For `INIT-AP-AI-FACTORY-001`, updated blocker framing to NVIDIA table access/participation constraint.
  - Marked NVIDIA delivery role as engaged with note that USG/Leo executed outreach and is pending response.
  - Logged structured `execution_updates` entry as `constraint_unblock_attempt` with provenance `provided_by_isaac`.

- Expanded Signals Feed source stack:
  - Added Google News RSS sources (ECOWAS public safety, Nigeria digital policing, AfDB/IFC/DFC security infra queries).
  - Added TheCable CBN remittance policy article as tracked source.
  - Added Nairametrics homepage as tracked source.

- Logged new signal in canonical signal register:
  - `SIG-2026-03-25-CBN-IMTO-NAIRA-SETTLEMENT-001` (CBN IMTO naira settlement directive), provenance `provided_by_isaac`, status Monitor.

- Signals Feed UI/UX updates:
  - Added source-count visibility + Google News quick filter affordance.
  - Implemented deterministic 4-column desktop card layout via explicit CSS grid.
  - Added short URL display (domain/path) instead of rendering massive raw URLs in cards.

- Signals ingestion hardening for Google News:
  - Decodes HTML entities and strips embedded anchor markup in RSS descriptions.
  - Attempts to resolve Google News redirect links to cleaner destination URLs where possible.

- Operational lesson captured:
  - Multiple `admin-server` processes were running concurrently at times, causing confusion over which process nginx upstream hit (`127.0.0.1:4180`).
  - Needed explicit live-listener verification before concluding UI patch was active.

- Workflow B reminder (11:00 UTC) executed:
  - Generated review packet `mission-control/review-packets/RP-0109-workflow-b-top-target-queue-2026-03-26.md`.
  - Current output confirms persistent top-10 DA/path coverage gaps and TAFF stale warming lane; no external actions taken.

