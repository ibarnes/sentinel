# 2026-03-27

- Isaac confirmed execution preference: **execution-first operator mode**. For actionable requests, run tools before replying; avoid interim status chatter; include proof-style completion summaries (tool used, file paths, result/errors).
- Isaac requested proactive completions via Telegram (do not wait for inbound ping). Preference captured and validated in-session.
- Dashboard improvements completed for UOS:
  - Added `/dashboard/actions` Action Register (flattened `meeting_minutes.next_actions` with filters, flags, export).
  - Added sidebar Actions link and moved Actions under **Control** (Meetings remains under Market Intelligence).
  - Enabled inline action status updates from Actions page and then changed UX to **auto-save on dropdown change** (no Save button).
- Meeting minutes pipeline updated:
  - Added new minutes entries into `dashboard/data/meeting_minutes.json` for Invest Africa roundtable, Angola AI Campus, and Eugene Yun meeting.
- Memory recovery work completed:
  - Recovered legacy `MEMORY.md` history from git into `memory/backups/memory-md-git/` with index files.
  - Rehydrated structured memory files: `memory/core.md`, `memory/active.md`, `memory/workspace.md`.
- Calendar integration established (read-only):
  - Added ICS importer `mission-control/calendar/import-upcoming-from-ics.mjs` and docs.
  - Calendar importer now auto-loads env from `admin-server/.env`.
  - Output path: `dashboard/data/calendar_events.json`.
- Daily readiness automation added:
  - Cron `daily-lock-load-brief` (06:45 ET)
  - Cron `meeting-prep-sweep` (T-24h/T-60m sweep window)
  - Cron `eod-execution-closeout` (20:30 ET)
  - Added `memory/calendar-prep-state.json` for dedupe/state tracking.
- HEARTBEAT fallback expanded with Calendar Readiness checks (import health, missing daily brief after 09:00 ET, missing prep evidence when meetings are within 24h).
- Known platform issue noted: semantic `memory_search` currently unavailable due embeddings API key/auth error; direct file-based memory remains usable.
