# Pipeline Run Credentialed Smoke Preflight — 2026-05-07T16:30:00Z

## Scope
- Parent stream: TASK-0097 / TASK-0103
- Goal: verify whether unattended runtime has authenticated inputs required for credentialed smoke.

## Required Inputs
- BASE_URL
- COOKIE
- DECK_ID

## Result
- status: BLOCKED
- reason: Authenticated runtime inputs are not present in unattended execution context.

## Next Operator Action
Run one-pass command during authenticated window:

```bash
BASE_URL="${BASE_URL}" \
COOKIE="${COOKIE}" \
DECK_ID="${DECK_ID}" \
bash scripts/pipeline-run-smoke.sh
```

## PASS Criteria (when executed)
- Valid payload returns `201` with non-empty `runId` and `status=started`.
- Invalid payload returns deterministic `400` contract failure.
